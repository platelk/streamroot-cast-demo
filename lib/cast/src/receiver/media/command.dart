part of cast.receiver.media;

@JS()
class Command {
  external static num get PAUSE;
  external static num get SEEK;
  external static num get STREAM_VOLUME;
  external static num get STREAM_MUTE;
  external static num get ALL_BASIC_MEDIA;
  external static num get QUEUE_NEXT;
  external static num get QUEUE_PREV;
}