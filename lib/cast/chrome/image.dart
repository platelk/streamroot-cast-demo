part of chromecast;

@JS()
class Image {
  external Image(String url);

  external num get height;
  external set height(num v);

  external String get url;
  external set url(String v);

  external num get width;
  external set width(num v);
}